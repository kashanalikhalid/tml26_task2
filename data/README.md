Place the released assets here.

- `target/`: one target checkpoint file or one model directory
- `suspects/`: one file or one directory per suspect model

The baseline script treats each top-level item inside `suspects/` as one suspect candidate.
