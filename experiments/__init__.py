"""Self-contained experiment scripts for TML 2026 Task 2.

Each experiment is a complete top-level driver — they intentionally do not
import each other. Library code lives in `detect/`. Each experiment writes
to its own subdir under `outputs/`.
"""
